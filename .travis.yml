sudo: required
services:
  - docker

before_install:
  - docker build -t lbakerjs/docker-react -f Dockerfile.dev .

language: generic

script:
  - docker run -e CI=true lbakerjs/docker-react npm run test